table:
  schema: public
  name: channels
select_permissions:
  - role: user
    permission:
      columns:
        - members
        - messsages
        - mode
        - name
        - id
      filter: {}
